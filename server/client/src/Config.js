// module.exports = {
//     apiUrl: `http://${window.location.hostname}:${3011}`,
//     socketUrl: `http://${window.location.hostname}:${3010}`
// }

module.exports = {
    apiUrl: `http://${window.location.hostname}:${3005}`,
    socketUrl: `http://${window.location.hostname}:${5000}`
}